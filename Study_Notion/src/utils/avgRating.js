export default function GetAvgRating(ratingArr) {
  if(ratingArr?.length === 0) {
    return 0;
  }


  // console.log("aarray", ratingArr)
  // let total = 0;
  // const totalReviewCount = ratingArr.forEach((single) => {
  //   total = total + single.rating;
  // })

  // console.log("avvv", total)

  // const avgReviewCount = total / ratingArr.length
  // return avgReviewCount
}